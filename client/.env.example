# Dev server (avoid 5173/5174 if those are in use)
VITE_DEV_PORT=5175

# Where the Express API runs (must match server PORT)
VITE_API_PROXY_TARGET=http://localhost:5051

# Optional: direct API base for production preview (no Vite proxy)
# VITE_API_URL=http://localhost:5051
